<h2> All about {{animal.name}}</h2>

<h3>Name: {{animal.name}}</h3>

{{#if animal.dob}}
<h3>Date of Birth: {{animal.dob}}</h3>
{{/if}}

<img src={{animal.imageurl}} alt="photo of the animal" style="height: 300px">

{{#if animal.hobbies}}
<h3>Hobbies:</h3>
{{/if}}
<ul>
  {{#each animal.hobbies}}
  <li>{{this}}</li>
  {{/each}}
</ul>

<a href='{{animal.name}}/edit'>
  <button type="button" class="btn btn-primary" style="margin: 0 5px;">Edit</button></a>

<a href='/animals/{{animal.name}}/delete' onclick="return confirm('are you sure you want to delete this animal?');">
  <button type="button" class="btn btn-danger" style="margin: 0 5px;">Delete</button> </a>

<style>
  #animalslist {
    font-weight: bold;
  }
</style>